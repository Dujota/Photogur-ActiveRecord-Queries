<%= link_to("Back to Pictures", "/pictures") %> | <%= link_to("Edit", "/pictures/#{@picture.id}/edit") %> | <%= link_to("Delete", pictures_path(@picture.id), method: :delete, data: {confirm: "Are you sure you want to delete this picture?"})  %>

<%= render 'show_picture', picture: @picture %>
